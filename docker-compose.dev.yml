services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    ports:
      - "4000:4000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
      - POSTGRES_HOST=db
      - POSTGRES_DB=luravanti
      - POSTGRES_USER=luravanti
      - POSTGRES_PASSWORD=devpassword
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    ports:
      - "5632:5432"
    environment:
      POSTGRES_DB: luravanti
      POSTGRES_USER: luravanti
      POSTGRES_PASSWORD: devpassword
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped

volumes:
  postgres_data_dev:
